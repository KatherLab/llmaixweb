### ── Build stage ───────────────────────────────────────────────────
FROM node:26-alpine AS builder

ARG GIT_COMMIT_HASH=unknown
ENV VITE_GIT_COMMIT_HASH=${GIT_COMMIT_HASH}

WORKDIR /app
# Copy the committed lockfile and install with `npm ci` so the image is built
# from the exact, tested dependency tree (reproducible + supply-chain safe).
# `npm install` re-resolved versions on every build, so the shipped image could
# diverge from what CI validated.
COPY package.json package-lock.json ./
RUN npm ci
COPY frontend ./frontend

RUN npm run build

### ── Runtime stage (non-root) ──────────────────────────────────────
FROM nginxinc/nginx-unprivileged:stable-alpine

USER root

COPY frontend/nginx.conf /etc/nginx/conf.d/default.conf
COPY frontend/docker-entrypoint.sh /docker-entrypoint.sh
COPY --from=builder /app/frontend/dist /usr/share/nginx/html

# Set proper ownership for nginx user (UID 101 for nginx-unprivileged)
RUN chown -R 101:101 /usr/share/nginx/html /etc/nginx/conf.d /docker-entrypoint.sh \
    && chmod +x /docker-entrypoint.sh

USER 101

ENTRYPOINT ["/docker-entrypoint.sh"]

EXPOSE 8080

CMD ["nginx", "-g", "daemon off;"]
