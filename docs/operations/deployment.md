# Deployment

This page is the production deployment handbook: reverse proxy, secrets,
storage, and the go-live checklist. It is included verbatim from the repository
root `DEPLOY.md`, which remains the single source of truth.

{%
   include-markdown "../../DEPLOY.md"
   heading-offset=1
%}
